# version: "3.8"

services:
  svelte:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: svelte_dev
    ports:
      - "4444:4444"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true