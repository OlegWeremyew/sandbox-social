# version: "3.8"

services:
  react:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: pixijs-opp
    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
