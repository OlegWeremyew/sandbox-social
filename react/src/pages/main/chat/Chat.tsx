import { type FC } from 'react';
import { MainContentWrapper } from '@/components';

const Chat: FC = () => {
  return <MainContentWrapper title="Chat">Chat</MainContentWrapper>;
};

export default Chat;
