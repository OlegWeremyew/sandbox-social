
# Работа с ветками

##  Наименование веток:

- `production (master)` — основная ветка, в которой всегда находится стабильная версия кода, готовая к релизу;

- `staging` — ветка для интеграции всех фич и изменений в тестовом окружении Здесь ведется тестировани;

- `development` — ветка для интеграции всех фич и изменений. Здесь ведется основная разработка;

- `feature/` — ветки для разработки конкретных задач или новых функций. Они создаются из develop и сливаются обратно после завершения работы;

- `hotfix/` — ветки для оперативного исправления критических ошибок в стабильной версии кода (в ветке main);

- `bugfix/` — ветки для исправления багов, обнаруженных в процессе разработки (создаются из develop).

## Типы изменений:

- `feature` — добавление нового функционала,

- `fix` — исправление ошибок,

- `refactor` — рефакторинг кода без изменения функциональности,

- `docs` — изменение или добавление документации,

- `test` — добавление или обновление тестов,

- `chore` — технические изменения, не влияющие на бизнес-логику.

---

# Проект социальной сети будет реализован на:
- frontend
  - react
  - vue
  - svelte
- backend
  - nest
  - express
  - golang
- mobile
  - react-native (in future)
